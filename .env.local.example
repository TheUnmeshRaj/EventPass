# Point to the running blockchain API server
NEXT_PUBLIC_BLOCKCHAIN_API=http://localhost:3001
# (Optional) If you run a public node, set RPC URL and private key in blockchain/.env
# RPC_URL=
# PRIVATE_KEY=
